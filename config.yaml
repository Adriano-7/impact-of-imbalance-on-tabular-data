experiment:
  imbalance_ratios: [1, 5, 10, 20, 50, 100]
  n_repetitions: 3
  random_state: 42  # Base seed for initial split
  test_size: 0.2

models:
  generators:
    - ddpm
    - nflow
    - ctgan
    - tvae

  random_forest:
    random_state: 42
    n_jobs: -1  

execution:
  # Parallel processing
  parallel:
    n_jobs: 4
    backend: loky

  resource_monitor:
    interval: 5

visualization:
  figure_size:
    width: 14
    height: 7

  color_palette: tab10

  generator_styles:
    ddpm:
      marker: o
      linestyle: '-'
      color: '#1f77b4'
    nflow:
      marker: s
      linestyle: '--'
      color: '#ff7f0e'
    ctgan:
      marker: '^'
      linestyle: '-.'
      color: '#2ca02c'
    tvae:
      marker: D
      linestyle: ':'
      color: '#d62728'
